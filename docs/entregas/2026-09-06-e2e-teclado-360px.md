# Entrega: E2E do fluxo crítico por teclado em 360 px

**Data:** 2026-09-06  
**Plano:** [E2E do fluxo crítico por teclado](../tasks/e2e-do-fluxo-critico-por-teclado.md)  
**PRD:** CA-17

## Funcionalidades

- Cenário Playwright Chromium em 360x760 com reduced motion.
- Seleção de liga, importação, confirmação, validação e preparação acionadas
  por teclado, com foco visível.
- Verificação de status/alert anunciados e ausência de overflow horizontal.

## Testes e validações

- `pnpm test:e2e -- --grep "fluxo crítico por teclado"`: aprovado (1 teste).
- `pnpm validate`: aprovado (testes, cobertura, contratos, typecheck, build,
  smoke, arquitetura, documentação e regras Firebase).

## Limites

A matriz adicional de Edge, Firefox e Safari não é instalada neste ambiente; o
contrato de acessibilidade observável fica coberto pelo Chromium local.
