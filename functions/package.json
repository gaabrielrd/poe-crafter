{
  "name": "@poe-crafter/functions",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "engines": {
    "node": "24"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --experimental-strip-types --experimental-specifier-resolution=node --test --test-isolation=none src/*.test.ts"
  },
  "devDependencies": {
    "typescript": "^6.0.3"
  },
  "dependencies": {
    "@google-cloud/vision": "^6.0.0",
    "@poe-crafter/planner": "workspace:*",
    "@poe-crafter/pricing": "workspace:*",
    "@poe-crafter/shared-types": "workspace:*",
    "firebase-admin": "^14.3.0",
    "firebase-functions": "^7.3.2"
  }
}
